import { FileExtenstionValidatorDirective } from './file-extenstion-validator.directive';

describe('FileExtenstionValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileExtenstionValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
