import { HighlightSearchedDirective } from './highlight-searched.directive';

describe('HighlightSearchedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightSearchedDirective();
    expect(directive).toBeTruthy();
  });
});
